language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-jpost' ]; then cd .. && eval "mv $currentfolder generator-jpost" && cd generator-jpost; fi

